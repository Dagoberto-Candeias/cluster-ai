services:
  backend:
    user: "${UID:-1000}:${GID:-1000}"
    command:
      - uvicorn
      - main_fixed:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
      - --reload-exclude
      - logs
